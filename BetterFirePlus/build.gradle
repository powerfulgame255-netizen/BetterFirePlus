plugins {
    id 'fabric-loom' version '1.7-SNAPSHOT'
    id 'maven-publish'
}

version = '1.0.0'
group   = 'com.betterfire'

base { archivesName = 'BetterFirePlus' }

repositories {
    maven { url 'https://maven.fabricmc.net/' }
}

loom {
    splitEnvironmentSourceSets()
    mods {
        "betterfirehud" { sourceSet sourceSets.main }
    }
}

fabricApi { configureDataGeneration() }

dependencies {
    // Minecraft + Fabric
    minecraft         "com.mojang:minecraft:1.21.4"
    mappings          "net.fabricmc:yarn:1.21.4+build.8:v2"
    modImplementation "net.fabricmc:fabric-loader:0.16.9"
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.110.0+1.21.4"

    // Gson is bundled with Minecraft, no extra dep needed
}

java {
    withSourcesJar()
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

jar {
    from('LICENSE') { rename { "${it}_${project.base.archivesName.get()}" } }
}
